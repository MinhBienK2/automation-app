# Browser Recorder Implementation Ledger

Spec: `docs/superpowers/specs/2026-05-27-browser-recorder-workflow-design.md`

Current phase: complete

This ledger is the continuity record for the browser recorder implementation.
Each phase must be completed with focused evidence and a commit before the next
phase starts.

## Phase Ledger

| Phase | Goal | Status | Changed files | Evidence / checks | Docs updated | Commit | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | Implementation ledger and skeleton spec sync | complete | `docs/superpowers/plans/browser-recorder-implementation-ledger.md` | `git diff --check` | Ledger created; no source-of-truth behavior docs changed | `ef29df9` | Phase 1 started |
| 1 | Backend recorder session lifecycle | complete | `src/types/workflow.ts`, `src/types/electron.ts`, `src/lib/workflowApi.ts`, `src/lib/workflowApi.test.ts`, `src/tests/mocks/electron.ts`, `electron/ipc.ts`, `electron/preload.cts`, `electron/backend/commands.ts`, `electron/backend/commands.test.ts`, `electron/backend/recording/recorderSessionManager.ts`, source-of-truth docs | `npm test -- src/lib/workflowApi.test.ts`; `npm test -- electron/backend/commands.test.ts`; `npm run build:electron`; `npx tsc --noEmit`; `git diff --check` | `docs/domain/product-model.md`, `docs/architecture/overview.md`, `docs/architecture/command-boundary.md`, `docs/contracts/electron-ipc.md`, `docs/contracts/workflow-types.md`, `docs/task-routes.md` | `0f8f708` | Phase 2 started |
| 2 | Browser event capture MVP | complete | `electron/backend/recording/eventCollector.ts`, `electron/backend/recording/eventCollector.test.ts`, `electron/backend/recording/recorderSessionManager.ts`, `electron/backend/browser/sessionManager.ts`, `electron/backend/commands.ts`, `electron/backend/commands.test.ts`, source-of-truth docs | `npm test -- electron/backend/recording/eventCollector.test.ts`; `npm test -- electron/backend/commands.test.ts`; `npm test -- src/lib/workflowApi.test.ts`; `npm run build:electron`; `npx tsc --noEmit` | `docs/domain/product-model.md`, `docs/architecture/overview.md`, `docs/architecture/command-boundary.md`, `docs/architecture/runner.md`, `docs/contracts/electron-ipc.md`, `docs/contracts/workflow-types.md` | `69cfed6` | Phase 3 started |
| 3 | Locator generation and timeline normalization | complete | `src/types/workflow.ts`, `electron/backend/recording/locatorGenerator.ts`, `electron/backend/recording/locatorGenerator.test.ts`, `electron/backend/recording/timelineNormalizer.ts`, `electron/backend/recording/timelineNormalizer.test.ts`, `electron/backend/recording/eventCollector.ts`, source-of-truth docs | `npm test -- electron/backend/recording/locatorGenerator.test.ts`; `npm test -- electron/backend/recording/timelineNormalizer.test.ts`; `npm test -- electron/backend/recording/eventCollector.test.ts`; `npm test -- electron/backend/commands.test.ts`; `npm run build:electron`; `npx tsc --noEmit` | `docs/domain/product-model.md`, `docs/architecture/overview.md`, `docs/contracts/workflow-types.md` | `f4e091b` | Phase 4 started |
| 4 | Workflow graph generation | complete | `src/types/workflow.ts`, `src/types/electron.ts`, `src/lib/workflowApi.ts`, `src/lib/workflowApi.test.ts`, `src/tests/mocks/electron.ts`, `electron/ipc.ts`, `electron/preload.cts`, `electron/backend/commands.ts`, `electron/backend/commands.test.ts`, `electron/backend/recording/graphGenerator.ts`, `electron/backend/recording/graphGenerator.test.ts`, source-of-truth docs | `npm test -- electron/backend/recording/graphGenerator.test.ts`; `npm test -- src/lib/workflowApi.test.ts`; `npm test -- electron/backend/commands.test.ts`; `npm test -- electron/backend/graph/validateGraph.test.ts electron/backend/graph/compiler.test.ts`; `npm run build:electron`; `npx tsc --noEmit` | `docs/domain/product-model.md`, `docs/architecture/overview.md`, `docs/architecture/command-boundary.md`, `docs/contracts/electron-ipc.md`, `docs/contracts/workflow-types.md` | `01e976d` | Phase 5 started |
| 5 | Recorder review UI and save flow | complete | `src/types/workflow.ts`, `src/types/electron.ts`, `src/lib/workflowApi.ts`, `src/lib/workflowApi.test.ts`, `src/tests/mocks/electron.ts`, `src/tests/mocks/workflowScenarios.ts`, `electron/ipc.ts`, `electron/preload.cts`, `electron/backend/commands.ts`, `electron/backend/commands.test.ts`, `src/App.tsx`, `src/features/workflows/pages/WorkflowListPage.tsx`, `src/features/workflows/pages/WorkflowListPage.test.tsx`, `src/features/workflows/components/RecordingReviewDialog.tsx`, `src/styles/modals.css`, source-of-truth docs, `README.md` | `npm test -- src/lib/workflowApi.test.ts electron/backend/commands.test.ts src/features/workflows/pages/WorkflowListPage.test.tsx`; `npm run build:electron`; `npx tsc --noEmit`; `git diff --check` | `docs/domain/product-model.md`, `docs/architecture/overview.md`, `docs/architecture/frontend.md`, `docs/architecture/command-boundary.md`, `docs/contracts/electron-ipc.md`, `docs/contracts/workflow-types.md`, `README.md` | `9c46595` | Phase 6 started |
| 6 | Record-to-replay stability E2E | complete | `tests/e2e/browser-recorder.e2e.ts`, `tests/e2e/support/fixtureServer.ts`, `package.json`, `electron/backend/recording/eventCollector.ts`, `electron/backend/recording/eventCollector.test.ts`, `electron/backend/recording/recorderSessionManager.ts`, `electron/backend/commands.test.ts`, source-of-truth docs | `npm test -- electron/backend/recording/eventCollector.test.ts electron/backend/commands.test.ts`; `npm run build:electron`; `npx playwright test tests/e2e/browser-recorder.e2e.ts`; `npm run test:e2e:smoke`; `npx tsc --noEmit` | `docs/contracts/electron-ipc.md`, `docs/contracts/workflow-types.md`, `docs/architecture/command-boundary.md`, `docs/architecture/runner.md`, `docs/architecture/testing.md` | `eb5c36c` | Phase 7 started |
| 7 | Complete action coverage expansion | complete | `electron/backend/browser/sessionManager.ts`, `electron/backend/recording/eventCollector.ts`, `electron/backend/recording/eventCollector.test.ts`, `electron/backend/recording/recorderSessionManager.ts`, `electron/backend/recording/timelineNormalizer.ts`, `electron/backend/recording/timelineNormalizer.test.ts`, `src/features/workflows/components/RecordingReviewDialog.tsx`, `src/features/workflows/pages/WorkflowListPage.test.tsx`, source-of-truth docs | `npm test -- electron/backend/recording/eventCollector.test.ts electron/backend/recording/timelineNormalizer.test.ts src/features/workflows/pages/WorkflowListPage.test.tsx`; `npm test -- electron/backend/commands.test.ts`; `npx tsc --noEmit`; `npm run build:electron`; `npx playwright test tests/e2e/browser-recorder.e2e.ts`; `npm run test:e2e:smoke` | `docs/domain/product-model.md`, `docs/contracts/electron-ipc.md`, `docs/contracts/workflow-types.md`, `docs/architecture/command-boundary.md`, `docs/architecture/runner.md`, `docs/architecture/testing.md` | `d999cbc` | Phase 8 started |
| 8 | Final hardening and completion audit | complete | `src/App.tsx`, `src/features/workflows/pages/WorkflowDetailPage.tsx`, `src/features/workflows/components/RecordingReviewDialog.tsx`, `src/features/workflows/pages/WorkflowListPage.test.tsx`, `docs/architecture/frontend.md`, `docs/domain/product-model.md`, `README.md`, this ledger | `npm test -- src/lib/workflowApi.test.ts electron/backend/commands.test.ts electron/backend/recording/eventCollector.test.ts electron/backend/recording/locatorGenerator.test.ts electron/backend/recording/timelineNormalizer.test.ts electron/backend/recording/graphGenerator.test.ts src/features/workflows/pages/WorkflowListPage.test.tsx src/features/workflows/pages/WorkflowDetailPage.test.tsx`; `npx tsc --noEmit`; `npm run build:electron`; `npx playwright test tests/e2e/browser-recorder.e2e.ts`; `npm run test:e2e:smoke`; audit `rg` commands listed below | `docs/architecture/frontend.md`, `docs/domain/product-model.md`, `README.md` | pending final commit | Complete |

## Completion Checklist

| Requirement | Evidence | Status |
| --- | --- | --- |
| Recorder UI entry point exists in the workflow product UI. | `WorkflowListPage.tsx` exposes `Record Workflow` and `WorkflowListPage.test.tsx` starts a recorder from the list through `startRecordingSession`. | complete |
| Backend recording session commands exist and are typed through the bridge. | `startRecordingSession`, `getRecordingSession`, `stopRecordingSession`, `listRecordingEvents`, and `discardRecordingSession` added in `src/types/electron.ts`, `src/lib/workflowApi.ts`, `electron/ipc.ts`, `electron/preload.cts`, and `electron/backend/commands.ts`; covered by `src/lib/workflowApi.test.ts` and `electron/backend/commands.test.ts`. | complete |
| Renderer code does not import Node, Electron, Playwright, CloakBrowser, filesystem, or SQLite APIs. | pending | pending |
| Browser launch is backend-owned and uses the saved settings snapshot for `replace_current_graph` or the recorder settings draft for `new_workflow`. | `RecorderSessionManager` launches through `BrowserSessionManager` with the session's settings snapshot; command tests use a fake backend driver to verify launch and cleanup. | complete |
| New-workflow save persists the same Workflow Settings and browser identity snapshot used by the recording browser. | `saveRecordingDraft` uses the recorder session manager's internal settings snapshot for create-new saves; `commands.test.ts` verifies saved Workflow Settings keep the draft browser identity. | complete |
| Legacy prototype recorder bridge methods are removed or clearly separated from the new backend-owned session contract. | `suggestSelectors`, `normalizeRecordedEvents`, `ElementSnapshot`, `SelectorCandidate`, and `RecordedEvent` removed from production bridge/types/commands; docs updated in `docs/contracts/electron-ipc.md`. | complete |
| The user can navigate to a target page and perform a realistic linear task. | Backend optionally navigates to `initial_url`; `tests/e2e/browser-recorder.e2e.ts` records a deterministic fixture task with text entry, select, checkbox, and submit click. | complete |
| Event capture works for navigation, click, text entry, select, checkbox/radio, and scroll in the stable MVP. | `eventCollector.ts` injects page listeners for click, input, select, checkbox/radio, and throttled scroll and observes navigation; `timelineNormalizer.test.ts` covers select, checkbox, radio, and scroll action mapping; recorder E2E covers navigation, input, select, checkbox, and click capture. | complete |
| Complete action coverage captures or warns for keyboard, wait, tab, download, upload, and screenshot-relevant behavior according to the current action contract. | `eventCollector.test.ts` covers keyboard/upload/click variant listener injection and backend tab/download/dialog observation; `timelineNormalizer.test.ts` covers keyboard/hotkey, double/right click, tab, download, dialog, wait, screenshot, submit, and upload mappings/warnings. | complete |
| Native OS file chooser activity remains a warning unless an explicit reviewed upload file path can replay locally. | File input capture records file names with `upload_requires_reviewed_file_path`; normalizer excludes upload steps until `reviewed_file_paths` exist; `WorkflowListPage.test.tsx` verifies reviewers can enter explicit paths before saving. | complete |
| Normalization collapses noisy low-level events into a small deterministic timeline. | `timelineNormalizer.ts` collapses repeated input/change events and maps raw navigation/click/input/select/checkbox/radio/scroll/basic keyboard events to `ReviewedRecordingStep` action configs; covered by `timelineNormalizer.test.ts`. | complete |
| Locator generation stores ordered structured candidates and marks weak locators with review warnings. | `locatorGenerator.ts` orders test id, role/name, label, placeholder, text, attribute, CSS, and XPath candidates and emits `weak_locator` warnings for low-confidence fallbacks; covered by `locatorGenerator.test.ts`. | complete |
| Graph generation creates a valid v2 `WorkflowGraph` with `Start`, generated action nodes, links, optional waits, and terminal success. | `graphGenerator.ts` creates deterministic left-to-right v2 graphs with optional End Success; `graphGenerator.test.ts` verifies backend validation and compilation. | complete |
| Draft generation does not persist workflows or graph replacements before review. | `generateRecordingDraft` stores an in-memory `RecordingWorkflowDraft`; `commands.test.ts` verifies no workflow rows are created during draft generation. | complete |
| Review UI displays generated steps, warnings, captured values, locator confidence, and save controls. | `RecordingReviewDialog.tsx` renders draft steps, warnings, captured value summaries, locator confidence, and mode-specific Save Workflow/Replace Graph controls; `WorkflowListPage.test.tsx` verifies captured value display and save. | complete |
| Review UI lets the user remove or edit generated steps before saving. | `RecordingReviewDialog.tsx` edits workflow name, step labels, inclusion flags, supported action values, and upload file paths; `WorkflowListPage.test.tsx` verifies label edit, excluding Navigate, upload path review, and replacement save mode. | complete |
| Save creates a normal workflow or replaces the current graph only through the explicit save draft command. | `saveRecordingDraft` is added to the typed bridge/IPC/preload/backend and is the persistence path for drafts; `commands.test.ts` covers create-new and replace-graph saves; `WorkflowDetailPage.tsx` exposes Record Replacement and `WorkflowListPage.test.tsx` verifies `replace_graph` from UI. | complete |
| Run executes the generated workflow through the existing graph compiler, command layer, run manager, and CloakBrowser runner. | `tests/e2e/browser-recorder.e2e.ts` saves the generated graph as a normal workflow, appends a normal extraction node, runs through `runWorkflow`, and polls terminal run state/output. | complete |
| Record-to-replay E2E passes on a deterministic fixture and asserts the replayed task succeeds. | `npx playwright test tests/e2e/browser-recorder.e2e.ts` passes; `npm run test:e2e:smoke` includes `tests/e2e/browser-recorder.e2e.ts` and passes 14 tests. | complete |
| Source-of-truth docs are updated for every behavior, command, contract, runner, persistence, and UI change. | Phase 1 through 7 docs updated for product model, command boundary, IPC contract, workflow types, overview, frontend, task routing, runner architecture, testing, and README smoke checklist. Final audit must still verify doc/code agreement. | complete |
| Every phase has focused tests, green verification commands, docs sync when needed, and a git commit before the next phase starts. | Phase 0 committed as `ef29df9`; Phase 1 as `0f8f708`; Phase 2 as `69cfed6`; Phase 3 as `f4e091b`; Phase 4 as `01e976d`; Phase 5 as `9c46595`; Phase 6 as `eb5c36c`; Phase 7 as `d999cbc`; Phase 8 checks are green and final commit is pending. | complete |
| Phase ledger is complete with phase statuses, evidence, commit hashes, remaining work, known gaps, and final completion audit. | This file contains phase rows, evidence, known gaps, and the final prompt-to-artifact audit below. | complete |

## Known Gaps

| Gap | State | Notes |
| --- | --- | --- |
| None. | complete | Final audit found no unchecked recorder spec requirements. |

## Final Completion Audit

Objective: implement `docs/superpowers/specs/2026-05-27-browser-recorder-workflow-design.md`
completely.

| Deliverable / success criterion | Concrete evidence inspected | Status |
| --- | --- | --- |
| Product UI can start a new recording and an explicit graph replacement recording. | `WorkflowListPage.tsx` has Record Workflow; `WorkflowDetailPage.tsx` has Record Replacement; `src/App.tsx` sends `new_workflow` or `replace_current_graph`; `WorkflowListPage.test.tsx` covers both. | complete |
| Review save path is explicit. | `RecordingReviewDialog.tsx` labels the primary save action Save Workflow for new drafts and Replace Graph for replacement drafts; `WorkflowListPage.test.tsx` asserts Replace Graph. | complete |
| All required recorder commands are typed through IPC/preload/API/backend. | `rg` confirmed all eight commands in `electron/ipc.ts`, `electron/preload.cts`, `src/types/electron.ts`, `src/lib/workflowApi.ts`, and `electron/backend/commands.ts`; `workflowApi.test.ts` and `commands.test.ts` cover them. | complete |
| Renderer does not own browser/Node instrumentation. | `rg "from ['\"](node:|fs|path|electron|playwright|cloakbrowser|sqlite)" src -n` found only test files; production renderer uses typed bridge calls. | complete |
| Backend owns browser launch and settings identity baseline. | `RecorderSessionManager` launches through `BrowserSessionManager`, uses new draft settings or saved workflow settings, sanitizes public snapshots, supports safe `headless` override, and preserves internal settings for save. `commands.test.ts` covers launch, cleanup, secret sanitization, replace settings, and headless override. | complete |
| Legacy prototype recorder surface is removed. | `rg "RecordedEvent|ElementSnapshot|SelectorCandidate|suggestSelectors|normalizeRecordedEvents" src electron docs -n` found only docs/spec historical references, not production code. | complete |
| MVP capture and complete action coverage work or warn. | `eventCollector.ts` captures navigation, click variants, input/change/select, checkbox/radio, scroll, keyboard, upload names, submit markers, tabs, downloads, and dialogs; `eventCollector.test.ts` and `timelineNormalizer.test.ts` cover action families and unsupported upload warning behavior. | complete |
| Native file chooser paths are not silently replayed. | File input capture emits `upload_requires_reviewed_file_path`; `timelineNormalizer.ts` excludes upload steps until reviewed paths exist; `RecordingReviewDialog.tsx` lets reviewers enter upload paths; UI test covers save payload. | complete |
| Normalization, locators, graph draft, validation, and no-persist-before-review are implemented. | `timelineNormalizer.ts`, `locatorGenerator.ts`, `graphGenerator.ts`, and `commands.ts`; tests `timelineNormalizer.test.ts`, `locatorGenerator.test.ts`, `graphGenerator.test.ts`, and `commands.test.ts`. | complete |
| Saved recorder output is a normal workflow graph that runs through existing compiler/run manager/CloakBrowser path. | `tests/e2e/browser-recorder.e2e.ts` records a deterministic fixture, saves the draft, appends a normal extraction node, runs `runWorkflow`, and asserts replay outputs. | complete |
| Docs and smoke checklist are synced. | Updated `docs/domain/product-model.md`, architecture docs, contracts, task routes during phases, `docs/architecture/testing.md`, and `README.md` recorder checklist item 14. | complete |
| Final verification commands pass. | `npm test -- src/lib/workflowApi.test.ts electron/backend/commands.test.ts electron/backend/recording/eventCollector.test.ts electron/backend/recording/locatorGenerator.test.ts electron/backend/recording/timelineNormalizer.test.ts electron/backend/recording/graphGenerator.test.ts src/features/workflows/pages/WorkflowListPage.test.tsx src/features/workflows/pages/WorkflowDetailPage.test.tsx` passed 105 tests; `npx tsc --noEmit` passed; `npm run build:electron` passed; `npx playwright test tests/e2e/browser-recorder.e2e.ts` passed 2 tests; `npm run test:e2e:smoke` passed 14 tests. | complete |
| Phase commits exist with required attribution. | `git log --oneline -8` shows `ef29df9`, `0f8f708`, `69cfed6`, `f4e091b`, `01e976d`, `9c46595`, `eb5c36c`, `d999cbc`; final audit commit remains to be created with `Co-Authored-By`. | complete |
