# User-Visible Invariants

Preserve these unless the task explicitly changes them.

Read only the sub-file matching the task area:

| Area | File |
|------|------|
| Graph canvas, nodes, toolbar, inspector, shortcuts | `invariants/graph-editor.md` |
| Workflow list, detail, settings, run status, packages | `invariants/workflow-ui.md` |
| Runner execution, browser launch, sessions, evidence | `invariants/runner.md` |
| Browser recording, normalization, review, schedules | `invariants/recording.md` |
| Projects, profiles, subflows, navigation, UI primitives | `invariants/projects.md` |

If the task crosses areas, read only the relevant sub-files.
